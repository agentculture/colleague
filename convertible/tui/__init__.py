"""Convertible TUI — terminal user-interface for the cockpit dashboard.

This package contains the state model (:mod:`convertible.tui.state`), event
definitions, reducer, renderer, and interactive driver that together form the
TUI cockpit.  All modules are stdlib-only with zero third-party dependencies.
"""
