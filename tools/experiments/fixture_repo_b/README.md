# calcpkg (experiment B fixture)

A deliberately tiny package. `divide` documents: a zero denominator returns
the sentinel `0.0` (it currently crashes — one test fails by design).
Run tests: `python -m pytest -q`.
