# purpose-tools-get-chosen

> Colleague's cortex chooses its purpose tools: the prompt no longer advertises delegation tools the acting seat does not hold, the raw alternatives that crowd the typed purposes out are removed where measurement says they should be, and every change lands as a pre-registered arm whose delegation rate is a tracked number
> instruction: verified by two artefacts: (1) a rendered pair - the acting seat's prompt and its offered tool list, showing no absent tool named and no offered purpose unnamed; (2) at least one completed docs/live-testing.md row whose delegation cell came from scripts/`compare_arms.py`, miss recorded as miss

## Audience

- The operator running cortex (Qwen3.8-27B) on the lobes-armed rig with `COLLEAGUE_ASSOCIATE_MODEL`=lobes and webglass on PATH, who shipped six purpose tools in v1.66.0 and has watched the model decline to call five of them across every measured run - and who has already decided (#435) that forcing delegation is not on the table, so the only remaining levers are what the model is told, what it is offered, and what the numbers say.
  - instruction: the spec names cortex/associate/scout by ROLE; no served model id appears in config code (the existing zero-model-ids boundary test still passes)

## Before → After

- Before: Before: the default prompt spends 174 words instructing the model to call subagent/subagents, which the acting seat does not hold, and 0 words on the six tools it does hold; the six purpose descriptions total 145 words, four carrying no steer and three carrying a steer-away clause; the only purpose prose that exists is a 49-word non-imperative armed-facts sentence on two of the six. Measured result: 0 purpose calls in 3 pre-registered runs plus 1 unregistered smoke (row 49), and delegation only where the raw alternative was removed (row 50).
  - instruction: recompute each number from source before writing the spec: word counts from colleague/prompttext.py and colleague/`purpose_schemas.py`, the delegation rate from docs/live-testing.md row 49
- After: After: a bare 'colleague work' prompt never names a tool the seat does not hold (option 1); the delegation rate for each option is a number in docs/live-testing.md rows 51+ with its brief committed beforehand and a miss written as a miss; the three levers are separable - prose-only, surface-only, and both - so the arm attributes the change to a cause instead of to the release.
  - instruction: the arm matrix in docs/live-testing.md has one row per lever with its own instrument cited (overlay sha256, env var, or allow-list diff), one shared brief, and one baseline; no row bundles two levers

## Why it matters

- 13-plus measured runs across rows 41-50 show cortex doing every survey itself; the associate seat is armed and idle. The one time it delegated (row 50) the raw alternative was gone. If prose cannot move the rate and surface composition can, that is a finding worth having as a number - it decides whether colleague's delegation design is a prompt problem or a surface problem, and #435's 'do not force it' means measurement is the only way to settle it.
  - instruction: the arc's closing doc names which lever moved the number and by how much, or states that none did - never 'inconclusive, needs more work' without the measured ratios beside it

## Requirements

- Option 1 (the stale-section fix): colleague/prompttext.py's `V1_DEFAULT_SYSTEM` and the adopted variant both carry a 174-word 'Subagents (optional)' section instructing the model to call subagent/subagents — two tools the top-level acting seat no longer holds after the t5 writer-allowlist swap. Rendering the real surface (loop.`resolve_role` -> role 'writer'; loop.`curated_schemas`) offers `web_survey`/`code_survey`/review/validate/plan/`handover_to_colleague` and NEITHER subagent nor subagents; the prompt names none of the six. The section must stop naming absent tools.
  - instruction: verified by rendering: uv run python -c "from colleague import loop,prompttext; ..." prints offered names + 'prompt mentions subagent: True' and False for all six purpose names
  - honesty: After the edit, rendering the bare acting seat shows the prompt naming exactly the tools the surface offers: 'subagent' and 'subagents' appear nowhere in the default prompt, and each of `web_survey` / `code_survey` / review / validate / plan / `handover_to_colleague` appears at least once.
- Every option lands as a PRE-REGISTERED arm in docs/live-testing.md written before any run, in the rows 47-50 shape: a verbatim brief file under docs/live-testing/briefs/, a pass bar committed up front (delegation-rate clause + turns <= 1.0x / wall <= 1.2x computed by scripts/`compare_arms.py` --bar-wall 1.2 --bar-turns 1.0 from artifact stats, never from prose), n=3 on each arm with the main baseline RE-RUN at the current tip, and a miss written as a miss.
  - instruction: the row exists with 'result: pending' before the first run; `compare_arms` output pasted, not prose
  - honesty: The row exists in docs/live-testing.md with 'result: pending' and its brief file committed under docs/live-testing/briefs/ BEFORE the first run of that arm - provable by the commit order in git log - and the completed row pastes `compare_arms.py` output rather than prose.
- The row-49 brief is a confound and must be re-authored: docs/live-testing/briefs/row49-purpose.md tells the model 'hand one or more of the read-only surveys to scout children (subagent / subagents)' while the branch arm's surface offers NEITHER tool - the brief advertises the same absent tools the stale prompt section does. A delegation arm must name the offered purpose tools or name no tool at all.
  - instruction: diff the new brief against row49-purpose.md; the words subagent/subagents appear nowhere in the branch-arm brief
  - honesty: The new brief contains no tool name that the arm's surface does not offer: grep for 'subagent' and 'subagents' in the branch-arm brief returns nothing.
- Option 2 (surface removal): the row-50 mechanism generalized - a purpose tool is chosen when the raw tool that does the same job is absent from the acting seat. Cortex today still holds `read_file`, `list_dir`, `grep_search`, glob and `run_command`, every one of which does `code_survey`'s job in-seat, which is exactly why row 49 measured 0/3. The option is to remove or narrow a NAMED set of those on the acting seat only (children keep them), behind a knob, and measure.
  - instruction: the arm's branch surface is printed from loop.`curated_schemas` and pasted into the row; children's surfaces are unchanged (actingsurface strips purpose tools at depth>=1, never read tools)
  - honesty: The surface-only arm changes nothing but the environment variable: git diff between the baseline tip and the arm tip is empty, and the row records the two rendered tool lists showing `grep_search` and glob present in one and absent in the other.
- The measurement instrument already counts the right thing and needs no change for options 1 and 3: scripts/`compare_arms.py`'s `DELEGATION_TOOLS` (:76-78) is the union of (subagent, subagents) and `PURPOSE_TOOL_NAMES`, imported live from colleague.`purpose_schemas` (:68), so `_count_delegations` (:199-201, from steps\[\].tool) already reports purpose calls; `_count_associate_calls` (:229-258) reads associate.`served_model`, steps\[\].`served_model`, steps\[\].arguments.`served_model` and `sub_results`\[\].role. Pass-bar flags are --bar-wall (default 0.7) and --bar-turns (default 0.8) and an arm passes when `wall_ratio` <= `bar_wall` AND `turns_ratio` <= `bar_turns` (:315) - rows 49/50 override to 1.2/1.0 explicitly.
  - instruction: verified by ask-colleague explore d40fed8ef720 (read-only, cited scripts/`compare_arms.py` line numbers); re-verify the line numbers before writing the row
  - honesty: scripts/`compare_arms.py` is not modified by this arc: git diff on that file across the arc is empty, and its delegations column is shown counting a purpose step on a real artifact.
- BLOCKER for option 3 as specced: `COLLEAGUE_PROMPT_SECTIONS` has no effect under the v1 variant. `default_system` returns `V1_DEFAULT_SYSTEM` before it ever reads sections (colleague/prompttext.py:530-539), and docs/features/adopt-from-qwen-code.md:335 states it outright - 'has no effect under `COLLEAGUE_PROMPT_VARIANT`=v1'. Since v1 IS the default since rows 43-44, an opt-in section can only be measured by also paying the whole adopted prompt (2.30x wall / 1.38x turns). Either sections must be made composable onto v1, or option 3's text lands inside the v1 literal - which is exactly the q2 move, merging options 1 and 3 into one edit.
  - instruction: verified in code and doc: prompttext.py:530-534 returns before the sections branch; adopt-from-qwen-code.md:335
  - honesty: The claim that `COLLEAGUE_PROMPT_SECTIONS` cannot reach the v1 default is demonstrated, not asserted: setting `COLLEAGUE_PROMPT_SECTIONS`=`HANDOVER_EXAMPLE` with the variant unset yields a prompt byte-identical to `V1_DEFAULT_SYSTEM`.
- Restoring subagent/subagents to the acting seat is now an ALLOWED fourth lever (operator, 2026-08-29) but it REVERSES a recorded design constraint and must be recorded as such, never slipped in: #443's 'Design constraint (from row 48): replace, don't add' and the measured basis for it (row 48: adding surface cost 3.31x wall / 1.41x turns with 2x reasoning chars and bought zero delegations). If restored, the existing 174-word prompt section becomes CORRECT with no prompt edit at all - which makes 'restore the tools' and 'fix the prompt' two alternative repairs of the same defect, and they must be measured as separate arms, never bundled.
  - instruction: if this lever is taken, CLAUDE.md's scope line and docs/features/purpose-tools.md record the reversal of 'replace, don't add' with the row that justified it and the row that overturned it
  - honesty: A restore-subagents arm and a fix-the-prompt arm are never run as one branch: each row names exactly one of the two repairs, and if both eventually ship the doc states which one the delegation number is attributable to.
- Encouragement STRENGTH is an experimental variable, not a fixed doctrine. The prose arm runs three rungs on the same overlay mechanism, same tip, same binary: P0 = no overlay (today's 18-word writer fragment); P1 = descriptive-corrected (names the six purpose tools, no imperative verb - the current armed-facts register); P2 = actively encouraging (imperative guidance plus a worked hand-over -> review -> collect example written with `code_survey`, the `HANDOVER_EXAMPLE` shape but naming tools the seat actually holds). This is the first time active encouragement is tested at all: `HANDOVER_EXAMPLE` exists but is opt-in AND names subagent, a tool the acting seat does not have.
  - instruction: three overlay files committed with the row, each cited by sha256; the arm reports the delegation rate at P0, P1 and P2 so the answer is a gradient, not a single point
  - honesty: The three rungs differ only in the overlay text: same tip, same env, same brief, and the row prints each overlay's sha256 alongside its delegation rate so P0/P1/P2 are attributable. If P2 raises turns without raising the delegation rate, that is recorded as delegation-for-its-own-sake and P2 fails.
- The surface arm CANNOT use `COLLEAGUE_TOOLS_LEGACY`=1 as specced: the knob is process-wide, consulted inside `curate_schemas` (tools.py:618) for EVERY role, so it strips `grep_search`/glob from the scout child too. Verified live: under the knob, writer goes 20->18 tools and scout goes 8->6, explorer/reviewer 9->7, validator 10->8. The arm would therefore cripple the delegate that `code_survey` spawns with the same knob meant to make delegating attractive - a confound that would most likely produce a WORSE result and be misread as 'delegation does not help'. The arm needs an ACTING-SEAT-SCOPED narrowing instead.
  - instruction: the arm asserts the two rendered surfaces explicitly: the acting seat lacks `grep_search`/glob AND the scout child still has them; a run where both lack them is void
  - honesty: The surface arm's row prints BOTH rendered surfaces - the acting seat without `grep_search`/glob AND the scout child still holding them - so a role-blind narrowing cannot be mistaken for a seat-scoped one.
- Arm 4 (restoring subagent/subagents) reaches FURTHER than the acting seat: actingsurface.`strip_purpose_tools` removes only the six purpose names at depth>=1, so putting subagent/subagents back into roles.`_writer_allowlist` also hands them to every spawned child. Verified live: a depth-1 child today is a BOUNDED writer - 15 tools, no subagent/subagents, no purposes (actingsurface docstring point 3 calls this deliberate). Arm 4 would silently undo that boundedness; nesting still terminates via `MAX_SUBAGENT_DEPTH`, but the change is not confined to the seat under test.
  - instruction: if arm 4 runs, it either strips subagent/subagents at depth>=1 the way purposes are stripped, or the row records that children regained delegation and that this is part of what was measured
  - honesty: Arm 4's row states the child surface explicitly: either children were stripped of subagent/subagents (matching today's bounded writer) or they were not, and the row says which was measured.
- The pass bar conflates two different outcomes and will mis-verdict the prose arm. c14 fails an arm whose turns rise, but #437's measured mechanism is precisely that prompt text buys DELIBERATION - so a P2 rung that succeeds at raising the delegation rate is expected to raise turns too. The arm must report the delegation clause and the cost clause SEPARATELY and name which one failed: 'delegation rose, cost bar missed' is a materially different finding from 'nothing happened', and collapsing them into one MISS loses the result this whole arc is trying to obtain.
  - instruction: the row records four cells per arm - delegation rate, turns ratio, wall ratio, reasoning chars - and the verdict line names the failing clause explicitly rather than a bare PASS/MISS
  - honesty: No row in this arc reports a bare PASS or MISS: every row names the delegation rate and the cost ratios separately, and a verdict that mixes them states which clause decided it.
- UNSTATED ASSUMPTION, and the pass's second headline: the arc treats a higher delegation rate as the goal, but nothing in the evidence shows delegation produces a BETTER outcome - and what evidence exists points the other way. Row 49's three non-delegating runs were all 'ok' and each changed a module; row 50's delegating run fired `web_survey` x3, consumed the whole 20-call web budget across three children, then step-stalled with NO final answer and no module changed. Every arm must therefore record TASK SUCCESS alongside the delegation rate, and the arc's conclusion must be allowed to be 'cortex was right not to delegate'.
  - instruction: each row records status (ok/incomplete/error) and whether the deliverable landed, per run; an arm that raises delegation while lowering success is written as a finding against delegation, not as a partial win
  - honesty: No row in this arc reports a delegation rate without the task-success column beside it, and the arc's closing doc states plainly whether delegating runs succeeded more, less, or equally often than non-delegating ones.
- The brief may be the confound, not the prompt or the surface: row 49's own recorded reading is 'on a three-small-file brief cortex reads the files itself', which describes RATIONAL behaviour - three small files are cheaper to read than to delegate. If the brief is below the size where delegation pays, every arm measures how hard we can push the model into a worse decision. The arc must include at least one brief demonstrably ABOVE that threshold (enough files or pages that in-seat reading exhausts context or budget) so the levers are tested where delegation is the correct choice.
  - instruction: the arm set includes a large-surface brief whose in-seat baseline demonstrably hits a budget or context limit; if no such brief is found, the arc records that it could not construct one
  - honesty: At least one arm runs on a brief where the non-delegating baseline provably struggles (hits the step budget, the context budget, or the web budget); a matrix run only on small briefs is reported as measuring small briefs only.
- A prose arm is NOT attributable from its artifact today, which defeats the arc's central lever. TaskResult records role (contract.py:1551-1556, serialized :1791-1792) and children carry SubResult.role (:364-367), but NO field holds the composed system prompt, any hash of it, or which env knobs were set - `config_digest` (:1717-1724) is a sha256 over `config_events` only. So a run under overlay P2 and a run under P0 are indistinguishable after the fact, and a mis-run arm is undetectable. The arc must add a prompt fingerprint (a sha256 of the composed system prompt) to the artifact, following `config_digest`'s precedent.
  - instruction: verified by ask-colleague explore f01e0d3fdfe4 (read-only, cited contract.py line numbers); after the change, two runs under different overlays show different prompt digests and the row cites the digest, not the file it believes was in place
  - honesty: Every row in this arc cites the prompt digest read back OFF the artifact, never the overlay file the operator believes was in place; a run whose digest does not match its arm is void.
- The overlay instrument does NOT reach a bare run, and the reason is a second prompt/surface drift. Engine.`system_prompt` (engine.py:140-204) reads config.role by NAME (:184) and only then composes the role fragment (:190); a bare 'colleague work' leaves config.role None, so the composed system prompt is EMPTY from the engine and the loop falls back to the roleless default. Probed live with a real .colleague/agents/writer.md: role=None -> `overlay_reached`=False, len=0; role='writer' -> `overlay_reached`=True, len=4059. Meanwhile actingsurface.`curate_for_depth` already substitutes the writer role for the SURFACE at depth 0. So the seat has the writer's tools and nobody's prompt - the same drift the arc exists to fix, in a second place.
  - instruction: the fix is to make the depth-0 writer substitution flow into the prompt as well as the surface, so one resolution feeds both halves; then a bare run and --role writer compose the same prompt AND the same surface, provable by rendering both
  - honesty: After the fix, a bare run and an explicit --role writer run compose an IDENTICAL system prompt and an identical tool surface - rendered and compared, not asserted; and the overlay reaches the bare run (the probe that returned False returns True).
- A dropped tool call is currently indistinguishable from a refusal to delegate, which threatens the validity of every zero-delegation row including row 49's 0/3. #360 (OPEN) measured the cortex emitting Hermes/Qwen tool-call markup as assistant TEXT on large-prompt turns - artifact 'steps: \[\]', '`stopped_without_finish`: true' - and states it outright: 'the failure is silent and looks exactly like the model ignored the tools'. colleague recovers literal markup ONLY for finish (loop.py:255-270, matching 'function=finish') and for the forced-synthesis turn (:274-296); markup naming any OTHER function, `code_survey` included, is neither recovered nor counted. The arc must detect and COUNT tool-call-shaped markup for any function name so a zero-delegation result can be told apart from a dropped one.
  - instruction: add a counter for markup-shaped tool calls in assistant content (any function name) onto the artifact; every arm row reports it, and a row with a nonzero count is not read as a refusal to delegate
  - honesty: A run in which the model emitted tool-call markup as text shows a nonzero markup counter on its artifact; a zero-delegation row states the markup count beside the delegation count, and a row with markup>0 is reported as inconclusive on delegation rather than as a refusal.
- The armed-facts sentence may be DISCOURAGING delegation rather than enabling it, and P2 must test the opposite framing. The shipped sentence (`delegation_text.py`:44-52) describes the scout as running 'considerably quicker', 'cannot write to the repository' and 'carries its reasoning switched off' - an accurate description that reads as a WEAKER helper, which is a reason for a careful model to keep the work itself. The P2 rung therefore tests capability-equal framing: that the child runs the SAME model family on a peer seat, that a second independent pass is worth having on its own (the diversity argument), and that the digest returns for review before anything is done with it.
  - instruction: P1 keeps the shipped descriptive sentence verbatim; P2 replaces the weaker-helper framing with the same-model / second-pass framing, and the two overlay files differ ONLY in that framing so the comparison isolates it
  - honesty: The P1 and P2 overlays differ ONLY in the capability framing (diffing them shows no other change), so a delegation-rate difference between them is attributable to that framing and nothing else.

## Honesty conditions

- On the armed rig, rendering the acting seat's prompt and surface together shows no tool named in the prompt that is absent from the surface, and no purpose tool absent from the prompt; and docs/live-testing.md carries at least one completed row whose delegation-rate cell was computed by scripts/`compare_arms.py`, with a miss recorded as a miss.
- The arm's stall exposure is stated as a number, not a caveat: every run in every arm records whether a stream guard tripped, and a row whose runs stalled reports the delegation rate over completed runs AND the stall count, never the rate alone.
- Each of the three levers is separately attributable: the prose-only arm and the surface-only arm differ from the baseline in exactly ONE variable each (the two prompt literals + snapshot, or `COLLEAGUE_TOOLS_LEGACY`=1), and the both arm is their union. If any arm changes more than its named variable the row says so.
- The bar is committed before the first run and computed by scripts/`compare_arms.py` --bar-wall 1.2 --bar-turns 1.0 from artifact stats; a prose arm that raises reasoning chars without raising the delegation rate is written as a fail, never as a draw or as 'directionally positive'.
- The arc answers 'prompt problem or surface problem' with a number, not a preference: if the prose arm and the surface arm both miss, that is recorded as a miss for both and the honest conclusion is that neither lever moves this model on these briefs.
- If the arc ships a permanent narrowing, the four exact-set pins are updated in the same commit as the allow-list change, with the changed expectation visible in the diff - never by relaxing an assertion to a subset check.
- docs/features/purpose-tools.md, docs/features/adopt-from-qwen-code.md (the prompt-variant knobs table and the ':257 delegation is never chosen' claim), CLAUDE.md's purpose-tools bullet and docs/live-testing.md are each updated in the same PR as the behaviour they describe, and none claims encouragement the shipped prompt does not carry.
- The before-state numbers are re-derivable at any time: the 174-word section, the 145 words of purpose descriptions, the 49-word armed-facts sentence and the 0-of-3 delegation rate can each be recomputed from colleague/prompttext.py, colleague/`purpose_schemas.py`, colleague/`delegation_text.py` and docs/live-testing.md row 49 without consulting this spec.
- The audience is named by ROLE and by measurement, not by aspiration: the operator can point at rows 41-50 for the zero-delegation history and at the row this arc adds for what changed. No claim is made about operators on rigs this arc never measured.
- Both Subagents literals stay in sync after the edit: tests/`test_prompttext.py`:71-74 (every V1 paragraph is a verbatim substring of the qwen prompt) passes without modification to the test itself.
- The chosen fix does not make the prompt vary per turn: `default_system` is still built once per run, and no call site rebuilds it inside the loop.
- The composed prompt for the acting seat names exactly the tools that seat is offered, and a spawned child's composed prompt does not name the purpose tools it cannot call - demonstrated by rendering both, not asserted.
- After the #438 work, a stalled turn on the blocking-fallback path trips a guard within its bound (demonstrated by a real-socket drip-feed test, not a mock), and the artifact carries a stream-guard trip counter that a live-testing row can cite without reading the warnings array.
- No arm in this arc changes what the runtime decides: every purpose call in every artifact is a model-emitted tool call, and the fixed purpose -> role -> seat -> rung table is unchanged.
- With no overlay present, the composed acting-seat prompt is byte-identical to the pre-arc composition in every arm - demonstrated by rendering, not asserted.
- All overlay files begin with the same effort line, and a run whose artifact shows a different acting-seat rung than the row claims is void.
- If the matrix is not completed, the arc's closing doc says which arms ran, which did not, and why - never presenting a partial matrix as the whole answer.

## Success signals

- The pass bar per arm: purpose calls >= 1 on >= 2 of 3 runs AND turns <= 1.0x / wall <= 1.2x vs the main baseline re-run at the current tip, computed by scripts/`compare_arms.py` --bar-wall 1.2 --bar-turns 1.0 from artifact stats. A prose-only arm that raises reasoning chars without raising the delegation rate is a fail, not a draw - that is #437's lesson applied.
  - instruction: run scripts/`compare_arms.py` --bar-wall 1.2 --bar-turns 1.0 over the arm's artifact ids and paste its output into the row; the delegations column is the delegation-rate cell

## Scope / boundaries

- Docs move with the code or not at all: docs/features/purpose-tools.md (Honest limits + Knobs table), docs/features/adopt-from-qwen-code.md (prompt-variant section), CLAUDE.md's purpose-tools bullet and docs/live-testing.md must state the measured delegation rate honestly, including a miss. The repo's doc-test-alignment discipline applies - no claim of encouragement the prompt does not carry.
  - instruction: run the doc-test-alignment skill before the PR; docs/features/purpose-tools.md, docs/features/adopt-from-qwen-code.md, CLAUDE.md and docs/live-testing.md are all in the same diff as the behaviour change
- FORCE and ENCOURAGE are different lines, and only force is excluded. #435's doctrine ('do not force delegation') is about the RUNTIME deciding - no automatic task-to-model routing, no harness-chosen child, the model still calls the tool explicitly. Imperative prose that recommends a purpose tool is not forcing: the model remains free to ignore it, and a wasted purpose call costs one step and a readable line (the purpose-tools h30 refusal-as-result rule). The descriptive-only register of `delegation_text`.`armed_facts` (previous frame c42/c44) was chosen for the RAW subagent surface and is not inherited by the typed purposes - it is superseded here for the overlay text specifically, and the reversal is recorded.
  - instruction: the runtime is unchanged in every arm: the AST guards in tests/`test_purpose_tools_boundary.py` and tests/`test_thinking_effort_boundary.py` pass, and no arm adds a code path that calls a purpose tool on the model's behalf
- The DEFAULT prompt still gains no words in this arc: all three prose rungs are delivered as operator role overlays (.colleague/agents/writer.md), so tests/snapshots/`prompttext_v1`.txt and `default_system` are untouched no matter how strongly P2 is worded. #437's cost lesson is honoured by keeping the text OUT of the default until a number justifies promoting it - which is c27's rule.
  - instruction: assert tests/snapshots/`prompttext_v1`.txt is unchanged by the PROSE ARM's instruments: the three overlays and the unification touch it not at all (git diff empty across t5/t12). The separate stale-section repair (t9, claim c2) regenerates it deliberately with `COLLEAGUE_UPDATE_SNAPSHOTS`=1 and says so in the PR - that is a different task with a different claim behind it.

## Non-goals

- Thinking continuity on the associate scout stays OUT (#446, OPEN): colleague never re-sends a prior turn's reasoning (loop.py:345-356), so `truncate_history_thinking` is a request-side no-op; carrying the scout's own `reasoning_content` back is a wire-shape change with its own unknowns (whether Nemotron 3.5 honours the kwarg; the context cost) and does not affect whether cortex CHOOSES a purpose tool.
- No routing policy, in any option: the runtime never picks a model, a role or a seat per turn. Each purpose stays fixed purpose -> fixed role -> fixed seat + rung (`PURPOSE_TABLE`), called explicitly by the model. Removing a raw alternative from the acting surface narrows what the model may call; it never decides for it.
- Not the #408 full tool-use audit: #408 (OPEN) asks for the complete action-surface audit - usage mining across every artifact, schema token overhead, batching reality, missing primitives, per-tool verdicts. This arc borrows only #408's item 5 (description tuning for reasoning minds, the `edit_file` precedent) for the tools it already ships; the mining sweep stays #408's.

## Assumptions

- Measurement is currently rate-limited by the rig, not by the design: row 50 proved the mechanism (cortex fired `web_survey` x3 in its first turn) but MISSED its bar because cortex step-stalled at step 7 on the 900 s stream-lifetime guard with no final answer - the #438 (OPEN) stall gap. Any arm long enough to show delegation is exposed to the same stall, so the arm design must either bound the brief below the stall window or record stalls as a first-class column.
  - instruction: the arm's row records stalls-cut / stalls-escaped per #438 guidance 5, alongside wall and turns
- Removing a raw tool from cortex PERMANENTLY (rather than behind a knob for one arm) costs four exact-set test edits: tests/`test_roles.py`:126-138 (`test_writer_allowlist_equals_schemas`, exact set equality), :140-155 (two-sided sync), :294-304 (`default_role`), tests/`test_purpose_tools_byte_identical.py`:88-100 (the e589451 delta pin - every non-dropped tool must survive), and tests/`test_agents_tools.py`:89-92 (`THINKER_CODER_TOOLS`/`ASSOCIATE_TOOLS` mirror). tests/`test_actingsurface.py`'s pins are disjointness-only and would survive.
- The SUBAGENTS paragraph is DUPLICATED: it is inlined inside the `V1_DEFAULT_SYSTEM` literal (colleague/prompttext.py:69-82) AND kept as the separate `_SUBAGENTS` constant (:124-139) used by the qwen variant and `SECTION_TABLE`. Editing it means editing both literals, regenerating tests/snapshots/`prompttext_v1`.txt (`COLLEAGUE_UPDATE_SNAPSHOTS`=1) and accepting that two pins named 'byte-identical to the v1.64.0 fixture' now describe a NEW fixture - tests/`test_prompttext.py`:141-146 and tests/`test_prompttext_handover_example.py`:38-44 - plus tests/`test_prompttext.py`:71-74, which asserts every V1 paragraph is a verbatim substring of the qwen prompt.
  - instruction: verified directly: sed -n on all four test sites; the assertions are as cited
- A surface-aware prompt (composing the delegation section from the tools actually offered) collides with two existing properties: `default_system` is built ONCE per run and prefix-stable (prompttext.py:508-528 'callers build it at run start and never per turn'), while colleague/resident/webtrust.py:153-196 narrows the offered surface PER TURN for a mesh turn. A prompt that mirrors the surface would either vary per turn (losing prefix stability and its cache) or describe the wrong surface on a narrowed turn.
  - instruction: if this option is chosen, the arm must record whether the prompt is rebuilt per turn and what it costs in cached-prefix terms
- \#438 is four small, separable changes, not a rewrite: bound the blocking fallback with the same StreamGuards (`vllm_openai.py`:719,782-783), gate backpressure's timeout doubling on guards-armed (loop.py:1928-1931), treat SSE keepalive lines as non-activity in the idle guard (streamguards.py:107-110), and extend runcounts.finalize to tally stream-guard trips alongside loop-guard trips (runcounts.py:76-85) so stalls-cut/stalls-escaped becomes an artifact number the arm rows can cite.
  - instruction: each of the four lands with its own test; the blocking-fallback one needs the real-socket drip-feed test #438 guidance 1 asks for
- Arm count and rig cost: the matrix is P0/P1/P2 prose x surface x both x restore = six arms plus one baseline, at n=3 that is 21 runs. Row 49's measured times (branch 88.6 s, main 327 s per run) put the serialized-GPU cost at roughly 1.5-3 hours before any stall, and #438's history is 5 hangs in 15 runs. The arc is feasible but is NOT a single-session measurement, and cutting n below 3 would make a binary delegation outcome unreadable.
  - instruction: the plan sequences the arms so the two cheapest and most decisive (P0 baseline, P2 prose) run first; a partial matrix is reported honestly as partial

## Scope exploration

- `s1` — `colleague/prompttext.py (V1_DEFAULT_SYSTEM lines 52-105; _SUBAGENTS lines 126-137; SECTION_TABLE lines 211-218; default_system lines 500-551)`: The v1 default (660 words) and the adopted variant (1622 words) both contain the 174-word SUBAGENTS section naming subagent/subagents; neither mentions any of the six purpose tools. `default_system` returns `V1_DEFAULT_SYSTEM` for any variant outside {qwen,adopted,qwen-handover}, so the stale section ships on every default run. Editing text inside `V1_DEFAULT_SYSTEM` touches the byte-identical floor pinned by tests/snapshots/`prompttext_v1`.txt.
  - seeds: `c2`
- `s2` — `colleague/delegation_text.py:26-60 (armed_facts / apply_armed_facts)`: The only purpose-tool prose that exists is one 49-word armed-facts sentence spliced onto `web_survey`/`code_survey` descriptions when the associate seat is armed; its docstring states the intent explicitly - 'it never tells the model to delegate, only describes the seat's nature' - with no imperative verb (decisions c42/c44). Confirms boundary c3: prose describes, never instructs.
  - seeds: `c3` (rejected)
- `s3` — `github issue #437 (OPEN) 'Prompt text is the cost'`: Measured arms: adopted prompt 2.30x wall / 1.38x turns (game) and 1.70x/1.60x (repo) vs main, ~3x reasoning chars; guidance 2 is to re-introduce adopted sections one at a time behind a measured arm on the same two briefs. This issue is the home for option 3 and the reason option 3 cannot be default-on.
  - seeds: `c4` (rejected)
- `s4` — `github issues #435 and #443 (both CLOSED, merged as PR #447 / v1.66.0)`: \#435 records the operator's standing doctrine 'do not force delegation - document it and adjust' and its candidate adjustment 1 is literally 'a short when-to-delegate section in colleague/prompttext.py, measured on a survey-first brief'. #443 restates it: 'nothing forces delegation; the choice costs one call'. Both closed issues remain the doctrine of record for this arc.
  - seeds: `c3` (rejected)
- `s5` — `docs/live-testing.md rows 49 and 50 (lines 113-114) + docs/live-testing/briefs/row49-purpose.md`: Row 49: purpose calls 0/3, `sub_results` 0 - MISS; recorded reading 'on a three-small-file brief cortex reads the files itself; the purpose form does not lower the ask enough to be chosen'. Row 50: cortex fired `web_survey` x3 in its FIRST turn when the raw web tool was absent from its seat - delegation happened exactly where the alternative was removed - but MISSED the bar on a step-stall at step 7. The two rows are the empirical split between option 2 (works) and option 1/3 (untested).
  - seeds: `c5`
- `s6` — `docs/live-testing/briefs/row49-purpose.md (the brief text, section 'The brief')`: The pre-registered brief names 'subagent / subagents' verbatim as the delegation affordance while the branch arm offers neither - the same absent-tool advertisement as the stale prompt section, inside the measurement instrument itself. Row 49's 0/3 result therefore cannot separate 'purpose form is unattractive' from 'the brief pointed at tools that were not there'.
  - seeds: `c6`
- `s7` — `github issue #438 (OPEN) 'Stall recovery' + docs/live-testing.md row 50 result`: \#438 documents 5 hangs in 15 game-benchmark runs, the 900 s stream-lifetime guard cutting 2 of 4, and two escaping via the blocking-fallback / raised-timeout path; its guidance 2 (retry-or-resume on a guard trip, knob `COLLEAGUE_STALL_RETRY` default 1) and guidance 5 (stalls-cut/stalls-escaped counter on the artifact) are exactly what row 50 needed and did not have.
  - seeds: `c7`
- `s8` — `github issue #446 (OPEN) + colleague/loop.py:345-356`: Thinking continuity is a wire-shape change on the scout's own history and is orthogonal to tool CHOICE; #446 already carves it out of the purpose-tools arc as frame decision q8 / non-goal c41. Confirms it stays out here.
  - seeds: `c8`
- `s9` — `github issue #408 (OPEN) 'Tool-use overview', item 5 (description tuning for reasoning minds)`: \#408 records that `edit_file`'s description 'actively routes traffic away from `write_file` and it works' and asks for the same intent applied to every description. That is the cheapest lever available to option 1/3 - the six purpose descriptions total 145 words today, four of the six carry no steer at all and three carry a steer-AWAY clause (use it for multi-file, and `read_file` for a single file).
  - seeds: `c2`
- `s10` — `colleague/purpose_schemas.py:60-95 (the six _DESC constants) + colleague/tools.py:582-631 (curate_schemas) + colleague/actingsurface.py:1-80`: Purpose descriptions total 145 words (`web_survey` 28, `code_survey` 34, review 18, validate 17, plan 20, `handover_to_colleague` 28) against the prompt's 174-word SUBAGENTS section pointed at absent tools; purpose schemas are appended by `curate_schemas` only when a concrete role resolves (allow is not None), and actingsurface.`curate_for_depth` substitutes `BUILTIN_ROLES`\['writer'\] for a bare None at depth 0 so a bare run and --role writer offer the identical surface.
  - seeds: `c2`
- `s11` — `colleague/roles.py:133-146 (_writer_allowlist) + colleague/search_schemas.py:51,122-133 + live render of loop.curated_schemas under COLLEAGUE_TOOLS_LEGACY=1`: The t5 swap dropped exactly {web, subagent, subagents}; every raw local-work alternative survives on cortex - `read_file`, `list_dir`, `grep_search`, glob, `run_command`, `run_tests`, `check_test_integrity`, `write_file`, `edit_file` - so `code_survey` competes with `grep_search`+`read_file`, validate competes with `run_tests` and review competes with `read_file`. `COLLEAGUE_TOOLS_LEGACY`=1 already removes the `grep_search`/glob pair uniformly, giving option 2 a knob-only arm.
  - seeds: `c11`
- `s12` — `tests/test_roles.py:126-155,294-304,352-367 + tests/test_purpose_tools_byte_identical.py:88-100,148,172,194-200 + tests/test_agents_tools.py:52,84,89-92 + tests/test_actingsurface.py:54-93,168,207,222`: Four files carry exact-set pins on the acting allow-list and one (`test_actingsurface.py`) carries disjointness pins only; a knob-scoped arm touches none of them, a permanent removal touches all four. This is the concrete cost difference between measuring option 2 and shipping it.
  - seeds: `c11`
- `s13` — `colleague/agents/tools.py:138-170 (PURPOSE_TOOLS profiles) + colleague/tools.py:634-687 (narrow_role_by_tool_set) + colleague/configlifecycle.py:104,173`: A second no-new-mechanism lever exists: agents mode's `WORKER_TOOLS` profile (`COLLEAGUE_AGENTS` + `agents_profile`='worker') already drops `write_file`/`edit_file`/`grep_search`/glob/culture/devague/`check_test_integrity` while keeping the six purposes; and EpisodeConfigSnapshot.`tool_set` threads an applied worker.tools narrowing through the SAME composed value that feeds both `curate_schemas` and ToolExecutor's refusal half. `THINKER_CODER_TOOLS`/`ASSOCIATE_TOOLS` are identical to the writer surface, so arming agents with those narrows nothing.
  - seeds: `c11`
- `s14` — `scripts/compare_arms.py:66-78,199-201,229-258,315,351-363 (explored via ask-colleague explore d40fed8ef720, read-only)`: delegations counts steps\[\].tool membership in `DELEGATION_TOOLS` = (subagent,subagents) + `PURPOSE_TOOL_NAMES` imported from colleague.`purpose_schemas`; `associate_calls` reads four served-model/role locations; the bar is two flags with defaults 0.7/0.8 that the purpose rows override to 1.2/1.0. No harness change is needed to measure any of the three options - only the row and the brief are new.
  - seeds: `c5`
- `s15` — `github issue #421 (OPEN, pre-registered thinking-effort arm) + #417 (cortex requests run at xhigh by default)`: A parallel pre-registered arm design already exists in the tracker (3 brief shapes x {off,medium,xhigh} x n=5) and #417 records that cortex runs at xhigh by default with low/medium not reliably cheaper - so effort is a live confound for any delegation arm: the same brief at a different rung may change whether cortex bothers to delegate. The arm must pin the rung explicitly rather than inherit it.
  - seeds: `c5`
- `s16` — `tests/test_prompttext.py:71-74,141-146 + tests/test_prompttext_handover_example.py:38-44 + tests/test_knobs_byte_identical.py:305-316 + tests/test_search_schemas.py:232-238 + tests/snapshots/prompttext_v1.txt (explored via ask-colleague explore 0182179b8abb, citations re-verified directly)`: Two byte-for-byte pins guard the default prompt and a third asserts each V1 paragraph is a verbatim substring of the qwen prompt; a fourth asserts '`grep_search`' never appears in `V1_DEFAULT_SYSTEM`. Any edit to the Subagents paragraph breaks all three prompt pins and requires a deliberate snapshot regeneration - the cost is a changed fixture identity, not a hidden break.
  - seeds: `c2`
- `s17` — `colleague/prompttext.py:530-539 (default_system early return) + docs/features/adopt-from-qwen-code.md:229-234,257,272,333-336`: The knobs table says `COLLEAGUE_PROMPT_SECTIONS` 'has no effect under `COLLEAGUE_PROMPT_VARIANT`=v1' and the code confirms it (the v1 early return precedes the sections read). adopt-from-qwen-code.md:257 also carries the standing delegation claim - 'Delegation is never chosen by the model on these briefs ... how much prompt guidance or role shaping that needs is a follow-up to adjust against these numbers' - which this arc is that follow-up to, and which must be updated with the measured rate.
  - seeds: `c24`
- `s18` — `docs/features/purpose-tools.md:46 + docs/features/adopt-from-qwen-code.md:39,222,229-234,257 (inventory partially delegated to ask-colleague explore de7f8d20b41a, which returned a meta-summary with the content dropped - re-done directly by grep)`: The docs make three claim families this arc must update: the prompt-variant default and its measured cost (adopt-from-qwen-code.md:222,229-234,333), the standing 'delegation is never chosen ... a follow-up to adjust against these numbers' claim (:257), and purpose-tools.md:46's note that cortex added unprompted `grep_search`/glob turns while the associate seat idled. No doc currently claims the model is encouraged to call a purpose tool - the docs are honest today and must stay so.
  - seeds: `c18`
- `s19` — `colleague/roles.py:196-215 (writer prompt_fragment), :279-300 (_resolve_role_prompt), :355-375 (overlay applied) + colleague/layers.py:674-704 (compose_role_prompt fixed order)`: Per-role prompts are a shipped, composed-per-role mechanism the arc can use instead of changing `default_system`: the writer fragment is 18 words, composes after AGENTS layers and before skills, and an operator file .colleague/agents/writer.md (or a per-model overlay) replaces it with no code change and no effect on the pinned v1 default. This is a strictly cheaper and better-targeted instrument than composable global sections.
  - seeds: `c31`
- `s20` — `colleague/streamguards.py:90-99,107-114,141-150 + colleague/engines/vllm_openai.py:53,360,574,719,782-783,1079 + colleague/loop.py:1722,1820-1841,1928-1931,3472-3510 + colleague/runcounts.py:41,76-85 (explored via ask-colleague explore 9a11eb6ccdcd, read-only)`: \#438's four guidance points map to four concrete sites: guards are armed only inside `_post_json_stream` (`vllm_openai.py`:574) from `COLLEAGUE_STREAM_IDLE_TIMEOUT` (240s) / `COLLEAGUE_STREAM_MAX_LIFETIME` (900s); the blocking fallback `_stream_or_blocking` (:719) falls back to a plain `_post_json` (:53) that never touches streamguards, so the guards genuinely do NOT bound it - bounded only by the urlopen request timeout; backpressure's `_escalate_request_timeout` (loop.py:1931 via `_make_timeout_escalator` :3472-3510) doubles config.timeout once and is NOT conditional on the guards being armed; and runcounts.finalize (:76-85) tallies only kind=='loop-guard' trips, so a StreamGuardTripped step-stall warning (loop.py:1820-1841) reaches the artifact as a warning but never as a counter.
  - seeds: `c28`
- `s21` — `challenge pass / adjacent-systems lens: colleague/tools.py:615-620 (curate_schemas consults search_schemas.offered for every role) + colleague/search_schemas.py:122-133 + live render of curate_schemas for writer/scout/explorer/reviewer/validator under COLLEAGUE_TOOLS_LEGACY=1`: The legacy knob is role-blind: it hides `grep_search`/glob from schema AND dispatch for every role in the process, so the surface arm's instrument degrades the scout child it is meant to make attractive (scout 8->6 tools). c25's zero-code arm is unsound as designed; this is the pass's headline finding.
  - seeds: `c41`
- `s22` — `challenge pass / lifecycle lens: colleague/actingsurface.py:74-90,111-121 + live render of curate_for_depth at child_depth=1 (child = bounded writer, 15 tools)`: The depth>=1 strip is purpose-name-only, so arm 4's restoration is not confined to the acting seat - children regain subagent/subagents and stop being bounded writers. Examined and found a real leak; recorded as c42 rather than left to surface mid-run.
  - seeds: `c42`
- `s23` — `challenge pass / failure-mode lens: colleague/roles.py:310-324 (_split_effort_frontmatter) + :279-300 (_resolve_role_prompt confinement, _within_config refuses a symlink out of .colleague/)`: Two findings: an overlay's leading 'effort:' line is consumed and validated (unknown rung raises CliError) - a hazard for free-text prose, converted into the instrument that pins the rung across arms (c43); and the overlay path is confined to the config dir with symlink escape refused and an unreadable file treated as absent, so the security lens is clean on this surface.
  - seeds: `c43`
- `s24` — `challenge pass / security lens: colleague/roles.py:279-300 (_resolve_role_prompt + _within_config) and the overlay content path`: Clean on the confinement question: an overlay resolving outside .colleague/ (a planted symlink) is refused, an unreadable file falls back to the built-in rather than an empty prompt, and only exact paths are tried (no sibling globbing). Residual: overlay TEXT is operator-authored and lands verbatim in the system prompt - trusted-operator model D2, unchanged by this arc.
- `s25` — `challenge pass / reversibility lens: the four arm instruments (overlay file, COLLEAGUE_TOOLS_LEGACY env, roles._writer_allowlist dropped-set edit, prompttext literals)`: Three of four instruments revert by deletion or unset with no residue; only arm 4 (allow-list edit) and the promotion step (writer fragment edit) are commits needing a revert commit, and neither touches persisted state or user data. No migration, no destructive operation, no data-loss surface in the arc - those c19 signals are absent, examined and recorded rather than assumed.
- `s26` — `challenge pass / containment lens: .colleague/agents/<name>.md placement`: An overlay is per-repo and silent: nothing in a run's output announces that a role overlay is active, so a file left behind in the colleague repo would silently shape every later run there. The arm must run in a throwaway repo and the plan must include removing the overlay; recorded as a park because no warning surface exists to fix cheaply.
- `s27` — `challenge pass / unstated-assumptions lens: docs/live-testing.md rows 49 and 50 outcomes (49: 3/3 ok, each changed a module, 0 delegations; 50: delegation observed, budget consumed, step-stall, no answer, no module changed)`: The frame assumed delegation is desirable and measured only whether it happens. The only two rows that bear on outcome point the other way - the non-delegating runs succeeded and the delegating run did not - so 'delegation rate up' is not evidence of improvement. Also surfaced: row 49's own reading describes cortex's non-delegation as rational for a three-small-file brief, which makes brief size a confound the arm set must control.
  - seeds: `c46`
- `s28` — `challenge pass / observability lens: colleague/contract.py:270-271,302-303,364-367,1481,1486,1551-1557,1687-1694,1704-1724,1725,1791-1792 (explored via ask-colleague explore f01e0d3fdfe4, read-only)`: The artifact identifies a run by engine, model, role, mode, command, `tip_sha`, `config_digest` and chain/continuation - enough to attribute a SURFACE arm, but nothing records the system prompt or a digest of it, so a PROSE arm is unattributable after the fact. This is the measurement-integrity gap for the arc's central lever; seeded c49.
  - seeds: `c49`
- `s29` — `challenge pass / operations lens: docs/live-testing.md rows 41-50 run history + #438 stall record (5 hangs in 15 runs)`: Examined the rig-operations surface: no migration, deployment or on-call dimension exists in this arc - the arms run as local processes against an existing rig - but run-completion is itself the operational hazard, already carried by c7/c34/c45 and the #438-first decision. Recorded as examined with no new finding beyond those.
- `s30` — `challenge pass / adjacent-systems lens (round 2): colleague/engine.py:140-204 (system_prompt reads config.role by name) + colleague/layers.py:744-751 + colleague/roles.py:368,373 + colleague/subagents.py:666 + colleague/oilcheck/three_tier.py:260 + colleague/purpose_schemas.py:66,494 + live probe with a real .colleague/agents/writer.md (explored via ask-colleague explore 57ef951d4fdf, probe re-run directly)`: One composition seam (Engine.`system_prompt` -> `compose_role_prompt`) and it keys on config.role BY NAME. Spawned subagent children do get their role's fragment; the three-tier worker seat and the agents-mode lobes seats do not (config.role unset); a `handover_to_colleague` child DOES (role='writer'). And a bare top-level run does NOT - so the depth-0 writer substitution reaches the surface but never the prompt. Probed: role=None `overlay_reached`=False len=0 vs role='writer' True len=4059.
  - seeds: `c50`
- `s31` — `challenge pass / measurement-validity lens: github issue #360 (OPEN) + colleague/loop.py:232-296 (_parse_literal_finish matches 'function=finish' only; _TOOL_MARKUP_MARKERS used for the synthesis guard)`: Literal tool-call markup is recovered for finish and guarded for the synthesis turn, but markup naming any other function is silently discarded and never counted - so #360's measured failure mode ('looks exactly like the model ignored the tools') is indistinguishable from a genuine refusal to delegate in every row this arc will produce, and retroactively in row 49.
  - seeds: `c51`
- `s32` — `challenge pass / counter-evidence lens: colleague/delegation_text.py:44-52 (the shipped armed-facts sentence)`: The sentence's accurate description - quicker seat, cannot write, reasoning switched off - reads as a weaker helper, giving a careful model a reason NOT to delegate. The frame had assumed the sentence was neutral-to-positive; it may be net negative, which makes capability framing a variable worth isolating rather than a wording detail.
  - seeds: `c52`

## Decisions

- q1: the option-2 arm removes `grep_search` and glob from cortex via `COLLEAGUE_TOOLS_LEGACY`=1 (`search_schemas.py`:51, consulted by `curate_schemas` at tools.py:618) - a knob-scoped arm, zero code and zero test edits, reversible. Permanence in `_writer_allowlist` is decided AFTER the number, never before.
  - instruction: the arm's row pastes the offered tool list rendered from loop.`curated_schemas` under both arms; the branch arm differs from the baseline by that one env var and nothing else
- q2: the stale-section fix edits BOTH literals - the inlined Subagents paragraph in `V1_DEFAULT_SYSTEM` (prompttext.py:69-82) and the `_SUBAGENTS` constant (:124-139) - so the section names the six purpose tools instead of subagent/subagents, and regenerates tests/snapshots/`prompttext_v1`.txt with `COLLEAGUE_UPDATE_SNAPSHOTS`=1. Net-zero words. The PR states the fixture identity change explicitly.
  - instruction: after the edit: uv run pytest tests/`test_prompttext.py` tests/`test_prompttext_handover_example.py` tests/`test_knobs_byte_identical.py` tests/`test_search_schemas.py` passes, and the regenerated snapshot is in the diff
- q3: the purpose-tools prose promotes to default-on when it passes its arm, on a rule pre-committed before the run - delegation rate up AND turns/reasoning chars not up (#437 guidance 2). Because `COLLEAGUE_PROMPT_SECTIONS` has no effect under variant v1, the prose lands inside the v1 literal itself (the q2 edit), so 'promote' means KEEP the edit and 'fail' means REVERT it - no new section mechanism is built.
  - instruction: the promotion rule is written into the live-testing row before the first run; the revert path is a single commit reverting the two literals + snapshot
- q4: #438 stall recovery lands FIRST, before any delegation arm runs - guidance 1-4 (bound the blocking-fallback / raised-timeout path with the same guards, `COLLEAGUE_STALL_RETRY` default 1, disable backpressure's timeout self-raise when stream guards are armed, treat SSE keepalive lines as non-activity) - so an arm can complete instead of repeating row 50's MISS-with-mechanism-proven.
  - instruction: a real-socket drip-feed test covers the blocking-fallback path; the arm rows record stalls-cut / stalls-escaped per run
- A per-role prompt is a BETTER home for the purpose-tool prose than a global section, and it needs no mechanism change at all: `BUILTIN_ROLES`\['writer'\] carries an 18-word `prompt_fragment` (roles.py:199-207) that composes after the AGENTS layers (layers.`compose_role_prompt`, fixed order at layers.py:695-699), and the writer role IS the resolved acting seat for a bare run. Because the prose then travels with the role that HOLDS the six tools, prompt and surface can never drift apart - the defect this whole arc exists to fix cannot recur by construction.
  - instruction: render the composed prompt for role 'writer' and for a child role and show the purpose prose present only where the purpose tools are offered
- The prose lever lives on the ROLE, not in a global section: the purpose-tool prose is the writer role's `prompt_fragment`, delivered for the arm as an operator overlay .colleague/agents/writer.md (roles.py:279-300,:368) and composed by layers.`compose_role_prompt` after the AGENTS layers. No change to `default_system`, no new `SECTION_TABLE` member, no snapshot regeneration - the pinned v1 default is untouched by the prose arm. This supersedes the composable-sections decision (c30, rejected).
  - instruction: baseline run asserts no .colleague/agents/writer.md exists; branch arm cites the overlay file's sha256; both arms run the same tip
- subagent/subagents restoration is a FOURTH arm in this arc, not a shipped default: the two tools return to the acting seat with the prompt left untouched (the existing 174-word section becomes correct by itself), and its delegation rate is measured against the prose and surface arms on the same brief. Row 48's 'replace, don't add' constraint gets a fair retest under purpose-tool conditions rather than being reversed on assertion.
  - instruction: the restore arm changes only roles.`_writer_allowlist`'s dropped set (and the four exact-set pins per c17); no prompt file changes in that arm
- Promotion target: a winning prose rung moves into `BUILTIN_ROLES`\['writer'\].`prompt_fragment` (colleague/roles.py:199-207), not into the global v1 literal. The text then ships for every operator without an overlay file, stays PER-ROLE so it never reaches a seat that lacks the purpose tools, and leaves tests/snapshots/`prompttext_v1`.txt untouched. This supersedes the q2/c26 plan of editing the v1 literal for the purpose-tool prose; the stale-SUBAGENTS repair is a separate question decided by the fourth arm.
  - instruction: on promotion: the writer fragment carries the winning text verbatim, tests/`test_roles.py`'s writer-role coverage is updated, and the v1 snapshot is NOT regenerated
- The overlay's effort frontmatter turns a hazard into the instrument that removes the #417/#421 confound: roles.`_split_effort_frontmatter` (roles.py:310-324) consumes a leading 'effort: <rung>' line from an operator role file and validates it, raising CliError on an unknown rung. So every prose overlay MUST begin with an explicit effort line - pinning the acting seat's rung identically across P0/P1/P2 - and the P0 baseline needs an overlay carrying ONLY that line plus the built-in writer sentence, so all four arms share one rung. Without it, an overlay whose first prose line happened to start with 'effort:' would either silently re-rung the seat or crash the run.
  - instruction: all overlays begin with the same 'effort: <rung>' line; the row records the rung; the P0 file reproduces the built-in writer sentence verbatim after that line
- The surface arm's instrument is a NEW acting-seat-scoped drop knob, not `COLLEAGUE_TOOLS_LEGACY`: a named drop-set applied at depth 0 only via tools.`narrow_role_by_tool_set` (tools.py:634-687), so the acting seat loses `grep_search`/glob while the scout child keeps them. Reuses the one composed value that already feeds `curate_schemas` and ToolExecutor's refusal half - no second refusal mechanism, no new narrowing engine.
  - instruction: the arm prints both rendered surfaces; a run where the scout also lacks `grep_search`/glob is void (h31)
- c50 is fixed by UNIFYING the two halves: the depth-0 writer substitution that actingsurface.`curate_for_depth` already applies to the tool surface must also feed the prompt, so one role resolution drives both and a bare run composes the same prompt AND surface as an explicit --role writer run. This is the arc's own thesis applied to the arc's own instrument.
- c51 lands as a COUNTER (markup-shaped tool calls for any function name, on the artifact), and row 49 is then RE-RUN as a validity check before the four levers are measured - the arc verifies its own motivating evidence rather than building on it. Recovering/executing dropped markup is explicitly NOT done in this arc: changing loop behaviour mid-experiment would confound every arm.
  - instruction: the row-49 re-run uses the same brief and n=3; if the markup counter is nonzero the original 0/3 is reported as inconclusive and the arc's framing is corrected in the docs

## Open parks

- [unknown_nonblocking] Whether option 1 alone moves the delegation rate at all is unknown: no arm has ever run with the prompt naming the tools the seat actually holds. Rows 41-50 all ran with the stale SUBAGENTS section, so 'prose does not work' has never been tested with CORRECT prose.
- [unknown_nonblocking] Whether removing `grep_search`/glob (option 2's knob arm) makes cortex reach for `code_survey` or simply fall back to `read_file`+`list_dir` walks is unknown - the pre-arc surface was the norm for every run before v1.64.0 and no purpose tool existed then to compete with it.
- [unknown_nonblocking] Whether n=3 is enough for a near-binary delegation outcome is unresolved: rows 49/50 used n=3 and row 49's 0/3 was unambiguous, but a rung that delegates on 1 of 3 runs is not distinguishable from noise at that sample size, and the serialized GPU makes n=5 expensive.
- [follow_up] No warning surface exists for an active role overlay - a .colleague/agents/writer.md left in a repo silently shapes every subsequent run's system prompt with nothing in the output or artifact announcing it. Out of scope to build here; a candidate for the doctor rubric.
