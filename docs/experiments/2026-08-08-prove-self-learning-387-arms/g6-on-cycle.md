# g6 ON-arm cycle record

- work: SINGLE leg (first since g1) — bb96109618f2, ok, 46 steps, PR #6;
  recall 5 records / 3149 chars (heaviest injection; 3 code-lessons riding)
- PR: https://github.com/OriNachum/transformer-arm-on/pull/6 → squash 089821d
- verification (subagent): criteria 1+3 PASS with exact-value evidence (door
  pushout plateau 20.750 = door.x − HERO_W/2 to 3 decimals; plate→d1 flip in
  the walk log; mesh visibly changed in screenshots); criterion 2 FUNCTIONAL
  FAIL — lever1 at x=24 sits BEHIND the door it opens (blocked left by closed
  d2, interact window walled above by the y=2 platform, every fall route
  respawns to x=2): d2 unopenable in any legal game state
- **TRANSFER (third consecutive): KeyE repeated both stored input patterns
  unprompted** — lowercase 'e'→'KeyE' normalization in both listeners + a
  KeyE latch slot. The input-correction class has not recurred since the
  lessons stored.
- INTEGRATOR CORRECTION (rule-bound, acceptance 2; data-only per the brief's
  data-driven wiring): lever1 x 24 → 20; empirically re-verified pre-merge —
  plate→d1 open at x=10.7, e at x=19.5 → d2 open, pass-through to x=22.4,
  lastError null throughout. Correction lines (tip f0409ae vs squash
  089821d): 2
- grade: 3; capture fired, 1 hunk, 1 lesson stored (store: 4 code-lessons —
  this one a LEVEL-DESIGN lesson: an opener must be reachable without the
  door it opens)
- anomalies: leaked worker server (killed); elevated-platform landing
  oscillation (pre-existing engine quirk) noted for g7/g8 criteria
