# g5 ON-arm cycle record

- work: 2-leg chain — 38c4147310f7 (budget-exhausted, 55 steps; recall 5
  records/2333 chars — largest injection, both code-lessons riding) →
  db91431b5ee2 (ok, 36 steps, PR #5)
- PR: https://github.com/OriNachum/transformer-arm-on/pull/5 → squash 2d2dedb
- **TRANSFER EVIDENCE (the arc's key observation so far): both stored
  code-lessons applied UNPROMPTED** — 'x' → 'KeyX' normalization symmetric
  with the inherited space fix, AND a KeyX slot in the MIN_HOLD_MS latch dict,
  each commented (main.js:35,40,45,54 per verification). The g3/g4 correction
  classes did NOT recur.
- verification (subagent): criteria 1 (marginal-pass: 10-of-9100 changed
  pixels — swing rotates toward the ortho camera, cosmetic, outside the bar),
  3, 4 PASS; criterion 2 FAIL — hit detection incremented the scene-side
  array; state.dummies (initialized []) had no writer.
- INTEGRATOR CORRECTION (rule-bound, acceptance 2): 1 line —
  state.dummies = levelGroup.userData.dummies; empirically re-verified
  pre-merge: hits 0 → 1 → 2 across strikes, hitFlash transient in state,
  lastError null. Correction lines (tip e9264bf vs squash 2d2dedb): 1
- grade: 3; capture fired, 1 hunk, 1 lesson stored (store: 3 code-lessons)
- anomalies: leaked worker server (killed); webglass ~1s extract latency can't
  see the 0.35s attack transient from a single press — concurrent press-burst
  probe recorded for OFF-arm reuse
