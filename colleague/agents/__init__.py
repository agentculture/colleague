"""Model-bound agents (#411) — the agent-side state and contracts; pure stdlib.

Typed profiles, purposes and role resolution (profile.py) plus the task ledger
(state/). No vendor model names anywhere under this package (test-pinned).
"""
